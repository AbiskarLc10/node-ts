name: Test Actions 
run-name: Abiskar is running workflows
on: [push]
jobs:
    First-job:
        runs-on: ubuntu-latest
        steps:
             - uses: actions/checkout@v5
             - name: Debug secret safely
               run: ./test.sh